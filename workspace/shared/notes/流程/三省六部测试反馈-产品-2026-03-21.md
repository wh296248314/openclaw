# 三省六部系统测试反馈（军机处·三省六部总控台）

**测试时间**：2026-03-21 04:35  
**测试人**：小皮管家（产品视角）  
**访问地址**：http://192.168.1.6:7891/  
**对应实例**：宿主机Python进程（PID 204319），工作目录 `/home/pixiu/edict`

---

## 一、测试结果总览

| 功能 | 状态 | 说明 |
|------|------|------|
| 主页加载 | ✅ 正常 | 「军机处·三省六部总控台」标题，完整渲染 |
| 旨意看板 | ✅ 正常 | 显示任务列表 |
| 省部调度 | ✅ 正常 | 监控面板 |
| 官员总览 | ✅ 正常 | 尚书令🟢活跃，模型正确 |
| 模型配置 | ✅ 正常 | set-model API工作 |
| 技能配置 | ✅ 正常 | |
| 档案馆 | ⚠️ 空 | 有1个文件（命名规范.md），无实际档案 |
| 奏折功能 | ℹ️ 前端存在 | Memorial tab，前端有渲染逻辑 |
| 圣旨模板 | ℹ️ 前端存在 | Template tab，可执行预定义模板 |
| 产出物检查 | ✅ 正常 | Doing→Review被拦截（无产出物） |
| 早报系统 | ✅ 正常 | 20260320版，5分类20条 |
| 模型信息 | ✅ 正确 | MiniMax-M2.5-highspeed（已更新） |

---

## 二、页面Tab结构

```
📜 旨意看板       → 任务列表
🔭 省部调度       → 监控面板
👥 官员总览       → 官员状态
⚙️ 模型配置       → 模型切换
🛠️ 技能配置       → 技能管理
📂 档案馆          → 归档文档（⚠️ 空）
📋 奏折           → 历史任务比较
📝 圣旨模板        → 快速创建任务
```

---

## 三、API测试详情

### ✅ 正常工作的API

```
GET  /api/live-status       → 正常（20任务，数据较新 04:09:29）
GET  /api/agent-config      → 正常（12 agents，模型正确 04:10:40）
GET  /api/officials-stats   → 正常（尚书令活跃，数据较新 04:10:33）
GET  /api/morning-brief     → 正常（20260320，5分类20条）
GET  /api/morning-config    → 正常（5个分类配置）
GET  /api/task-activity/{id}→ 正常（返回25条流转记录）
POST /api/set-model         → 正常（Queued反馈）
POST /api/review-action     → 正常（任务存在时）
```

### ⚠️ 异常的API

```
GET /api/archives-list     → ⚠️ 空（只有1个文件：命名规范.md）
GET /api/memorials         → ❌ 404（路由不存在）
GET /api/templates         → ❌ 404（路由不存在）
GET /api/tasks-stats       → ❌ 404（路由不存在）
GET /api/schema             → ❌ 404（路由不存在）
```

---

## 四、发现的问题

### 🟡 问题1：档案馆几乎为空

**现状**：
- 扫描目录：`~/.openclaw/workspace/archives/`
- 实际文件：只有 `命名规范.md` 和 `.git/` 文件夹
- 无实际任务归档文档

**对比**：昨天（3月20日）皮休同步说"档案馆功能完善"，但实际归档内容为空

**建议**：需要填充实际的任务产出文档，或配置其他归档目录

---

### 🟡 问题2：奏折/模板功能只有前端，无API

**现状**：
- Memorial tab 和 Template tab 在前端HTML中有完整渲染代码
- 但 `/api/memorials` 和 `/api/templates` 返回 404
- 后端未实现这些API

**说明**：可能是皮休计划中的功能，前端先搭好了

**建议**：确认是否需要后端实现

---

### 🟡 问题3：/api/tasks-stats 返回404

**现状**：该API在 localhost:7891(Docker) 返回数据，但在 192.168.1.6:7891(主机) 返回404

**说明**：两个edict实例的API实现略有不同

---

### 🟢 正常：产出物检查机制有效

```
测试：Doing → Review（无产出物）
结果：⚠️ JJC-20260319-001 无产出物，禁止从 Doing → Review
提示：请先提交产出物（用 python3 kanban_update.py done ...）
```

---

## 五、与 localhost:7891（Docker）的区别

| 对比项 | localhost:7891 (Docker) | 192.168.1.6:7891 (Host) |
|--------|-------------------------|--------------------------|
| 标题 | 三省六部·Edict Dashboard | 军机处·三省六部总控台 |
| 工作目录 | 容器内 `/app` | 宿主机 `/home/pixiu/edict` |
| 文件系统 | 独立（需手动同步） | 直接访问宿主机 |
| archives-list | 空 `{"files":[]}` | 有1个文件（实际扫描正确） |
| officials-stats | 数据旧（2月24日） | 数据新（已同步） |
| task-activity | ❌ 不存在 | ✅ 正常 |
| 进程 | Docker container | Python directly |

**关键发现**：192.168.1.6:7891 是**主实例**，数据更新、更完整。

---

## 六、建议

| 优先级 | 问题 | 建议 |
|--------|------|------|
| 🟡 中 | 档案馆为空 | 确认归档流程，填充任务产出文档 |
| 🟡 中 | Memorial/Template无后端 | 确认是否需要实现 |
| 🟢 低 | 两个实例API不一致 | 考虑统一或文档说明 |
| 🟢 低 | tasks-stats 404 | 确认该API是否必需 |

---

## 七、测试命令

```bash
# 主机实例
curl http://192.168.1.6:7891/api/live-status
curl http://192.168.1.6:7891/api/officials-stats
curl http://192.168.1.6:7891/api/archives-list
curl http://192.168.1.6:7891/api/task-activity/JJC-20260319-001

# 检查进程
ps aux | grep dashboard/server.py | grep -v grep
# PID 204319, 监听 0.0.0.0:7891
```

---

*产品视角测试完成 | 小皮管家 | 2026-03-21 04:45*
